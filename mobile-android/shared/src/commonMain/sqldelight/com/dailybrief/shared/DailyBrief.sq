CREATE TABLE events (
  id TEXT NOT NULL PRIMARY KEY,
  title TEXT NOT NULL,
  topic TEXT NOT NULL,
  summary TEXT NOT NULL,
  score REAL NOT NULL
);

insertItem:
INSERT OR REPLACE INTO events(id, title, topic, summary, score)
VALUES (?, ?, ?, ?, ?);

selectAll:
SELECT * FROM events;
